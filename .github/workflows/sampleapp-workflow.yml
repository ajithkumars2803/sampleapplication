name: Build and Push Docker Image to ECR using OIDC

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-1
      ECR_REPOSITORY: sampleapplication
      AWS_ACCOUNT_ID: 160885263219
      IMAGE_TAG: ${{ github.sha }}

    steps:
    - name: Checkout source
      uses: actions/checkout@v3

    - name: Configure AWS credentials with OIDC
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: arn:aws:iam::160885263219:role/githubaction
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      run: |
        
